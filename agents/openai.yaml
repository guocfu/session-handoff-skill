interface:
  display_name: "Session Handoff"
  short_description: "Save and resume project context across Codex sessions/providers."
  default_prompt: "Save or load SESSION_HANDOFF.md so a new Codex session can continue the project."
