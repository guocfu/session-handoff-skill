interface:
  display_name: "Session Handoff"
  short_description: "Save and resume concise handoffs across agents."
  default_prompt: "Use $session-handoff to read SESSION_HANDOFF.md in read-only mode and summarize the current project state."
